Invoke-RestMethod "http://localhost:8000/audit?limit=20" |
  Select-Object -ExpandProperty entries |
  Where-Object { $_.operation -like "*ROLLBACK*" -and $_.success -eq $false } |
  ConvertTo-Json -Depth 6
